services:
  - type: web
    name: recipe-server
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: python production_server.py
    envVars:
      - key: OPENAI_API_KEY
        sync: false
